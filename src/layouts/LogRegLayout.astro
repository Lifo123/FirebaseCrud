---
import '@Assets/css/LogReg.css';
import { Toaster } from 'sonner';

---

<script is:inline>
    let user = JSON.parse(localStorage.getItem('F-User'));
    if (user) {
        window.location.href = '/Games/'; 
    }
</script>

<Toaster client:only="react" richColors expand position='top-center'/>
<slot />
